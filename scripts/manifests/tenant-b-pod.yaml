apiVersion: v1
kind: Pod
metadata:
  name: tenant-b-pod
  labels:
    tenant: tenant-b
  annotations:
    # In production, CNI meta-plugin would read this and set fwmark
    tenant.routing/fwmark: "0x20"
spec:
  containers:
  - name: test
    image: nicolaka/netshoot:latest
    command: ["sleep", "infinity"]
  nodeSelector:
    kubernetes.io/hostname: k8s-node-0
