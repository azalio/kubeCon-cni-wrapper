apiVersion: v1
kind: Pod
metadata:
  name: tenant-a-pod
  labels:
    tenant: tenant-a
  annotations:
    # In production, CNI meta-plugin would read this and set fwmark
    tenant.routing/fwmark: "0x10"
spec:
  containers:
  - name: test
    image: nicolaka/netshoot:latest
    command: ["sleep", "infinity"]
  nodeSelector:
    kubernetes.io/hostname: k8s-node-0
